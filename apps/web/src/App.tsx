/**
 * Reserved for future app-level composition. The app currently mounts through
 * `router.tsx` (see `main.tsx`); this file is kept as the conventional entry
 * point for shared providers that shouldn't live in the router tree.
 */
export {};
