# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# env / secrets
.env
.env.local
.env.*.local

# drizzle
packages/db/drizzle/meta/_journal.json.bak

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
.claude/*
# launch.json is shareable project config (dev-server/preview), not local settings.
!.claude/launch.json
# Shared project skills are committed (course-deck-builder etc.); local
# settings/state under .claude stay ignored.
!.claude/skills
!.claude/skills/**

# design reference bundle (source of truth, not app source)
High fidelity app prototype.zip
project/

# local scratch PDFs for manual pipeline testing
TestPDFS/

# vite / test
apps/web/.vite/
coverage/

# workforce migration data — contains PII, never commit
chc-*.csv

# extraction-tuning corpus — real, safety-critical assessment PDFs, never commit
corpus/

# Emitted by author-small-loader-tool.mjs on older runs (now written to the temp dir)
packages/db/small-loader.deck-questions.json
